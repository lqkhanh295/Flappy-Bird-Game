<Window x:Class="PRN212.G5.FlappyBird.SelectSkin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRN212.G5.FlappyBird"
        mc:Ignorable="d"
        Title="Select Your Bird Skin" 
        Height="550" 
        Width="750"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFF5E2">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Choose Your Bird"
                   FontSize="38"
                   FontWeight="Bold"
                   Foreground="Black"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,25"/>

        <!-- Skins Panel -->
        <WrapPanel x:Name="SkinsPanel"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Back Button -->
        <Button Grid.Row="2"
                x:Name="BackButton"
                Content="Back"
                Width="160"
                Height="50"
                FontSize="18"
                FontWeight="Bold"
                Margin="0,15,0,0"
                HorizontalAlignment="Center"
                Background="#D6B37C"
                Foreground="Black"
                BorderThickness="0"
                Cursor="Hand"
                Click="BackButton_Click"
                MouseEnter="BackButton_MouseEnter"
                MouseLeave="BackButton_MouseLeave"/>
    </Grid>
</Window>
